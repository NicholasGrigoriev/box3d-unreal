#include "Modules/ModuleManager.h"

/// Editor-only tooling for the Box3D integration; currently the Box3DBake
/// commandlet (see docs/BAKED_COLLISION.md).
IMPLEMENT_MODULE(FDefaultModuleImpl, Box3DEditor);
